Linked list assignment from [The Odin Project](https://www.theodinproject.com/).

Migrated to TypeScript and Bun from JavaScript and Node.js.

Test
```bash
bun test
```
Run the example
```bash
$ bun ./index.ts
( dog ) -> ( cat ) -> ( parrot ) -> ( hamster ) -> ( snake ) -> ( turtle ) -> null
```
